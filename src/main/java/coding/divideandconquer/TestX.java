package coding.divideandconquer;

//@State(value=Scope.Thread)
public class TestX {
	
/*	DivideAndConquerI divide = new DivideAndConquer();;

	@Before
	public void init() throws Exception {
		Class<?> clazz = Class.forName("coding.divideandconquer.DivideAndConquer");
		Constructor<?>[] constructors = clazz.getConstructors();
		divide = (DivideAndConquerI) constructors[0].newInstance();
	}

	@Test
	public void testBinarySearch() {
		int[] arr = new int[] { 1, 2, 3, 4, 5, 6 };

		for (int k : arr) {
			int resp = divide.binarySerach(arr, k);
			assertEquals(k, resp);
		}

		int resp = divide.binarySerach(arr, 10);
		assertEquals(-1, resp);
	}

	
	@Benchmark
	@BenchmarkMode(Mode.AverageTime)
	@OutputTimeUnit(TimeUnit.NANOSECONDS)
	@Fork(value=1)
	@Warmup(iterations=1)
	@Threads(value = 4)
	@Test
	public void testSum() {
		
		int[] arr = new int[] { 1, 2, 3, 4, 5, 6 };
		int resp = divide.sum(arr);
		assertEquals(21, resp);
	}
	
	@Benchmark
	@BenchmarkMode(Mode.AverageTime)
	@OutputTimeUnit(TimeUnit.NANOSECONDS)
	@Fork(value=1)
	@Threads(value = 4)
	@Warmup(iterations=1)
	@Test
	public void testSumStream() {
		
		int[] arr = new int[] { 1, 2, 3, 4, 5, 6 };
		int resp = divide.sumStream(arr);
		assertEquals(21, resp);
	}*/
}
