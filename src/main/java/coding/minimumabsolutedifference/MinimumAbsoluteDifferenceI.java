package coding.minimumabsolutedifference;
/**
 * Given an array of integers, find and print the minimum absolute difference between any two elements in the array. 
 * For example, given the array arr = [-2,2,4] we can create 3 pairs of numbers (-2,2), (-2,4), (2,4).  
 * The absolute differences for these pairs are: 4,6,2. The minimum absolute difference is 2. 
 * @author rodrigo ruiz
 *
 */
public interface MinimumAbsoluteDifferenceI {
	int minimumAbsoluteDifference(int[] arr);
}
